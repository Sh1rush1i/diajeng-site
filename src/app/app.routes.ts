import { Routes } from '@angular/router';
import { LandingConfirmation } from './component/main/landing-confirmation/landing-confirmation';

export const routes: Routes = [{ path: '', component: LandingConfirmation }];
